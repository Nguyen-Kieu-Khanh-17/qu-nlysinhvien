# TODO - Sửa giao diện

- [x] Thay trang `/student-manager` bằng giao diện đẹp hơn (giống dashboard CRUD ở `static/index.html`).
- [x] Đồng bộ controller để `/student-manager` trả về đúng template mới.
- [x] Đảm bảo nút/JS gọi đúng API và hiển thị avatar qua endpoint `/uploads/**`.

- [ ] Chạy kiểm tra build/test (nếu Maven có thể chạy trên máy).



